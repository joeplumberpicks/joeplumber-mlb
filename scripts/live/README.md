# Live NRFI Utilities

Pregame live workflows use a **schedule spine** so daily runs can execute before Statcast plate appearances are available.

- Spring Training is supported via MLB `gameType=S` schedule ingest.
- Regular Season is supported via MLB `gameType=R` schedule ingest.
- The NRFI model is trained on regular-season data; spring training support is intended for pipeline validation/testing.
